import { I{{ pascalCase usecase_name }}UseCase } from '@/domain/usecases/{{ kebabCase (or selected_module usecase_name) }}/{{ kebabCase usecase_name }}.usecase.interface';
{{> usecaseOnlyRepoImport }}

export class {{ pascalCase usecase_name }}UseCase implements I{{ pascalCase usecase_name }}UseCase {
  {{> usecaseOnlyRepoConstructor }}
  async execute(data: I{{ pascalCase usecase_name }}UseCase.Input): Promise<I{{ pascalCase usecase_name }}UseCase.Output> {
    console.log(data);
    return 'UseCase executed';
  }
}
