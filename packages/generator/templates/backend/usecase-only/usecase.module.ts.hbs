import { InfraModule } from '@/infra/_infra.module';
import { Module } from '@nestjs/common';
import { {{ pascalCase usecase_name }}UseCase } from './{{ kebabCase usecase_name }}.usecase';

@Module({
  imports: [InfraModule],
  providers: [
    {
      inject: [],
      provide: {{ pascalCase usecase_name }}UseCase.name,
      useFactory: () => new {{ pascalCase usecase_name }}UseCase(),
    },
  ],
  exports: [{{ pascalCase usecase_name }}UseCase.name],
})
export class {{ pascalCase usecase_name }}Module {}
