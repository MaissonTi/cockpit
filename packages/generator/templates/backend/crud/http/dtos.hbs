import { ApiProperty } from '@nestjs/swagger';
import { IsNotEmpty, IsOptional, IsString, IsUUID } from 'class-validator';
import { PaginationQueryDTO, PaginationResponseDTO } from './_pagination.dto';

export class {{ pascalCase usecase_name }}RequestDTO {
  @IsString()
  @IsNotEmpty()
  name: string;
}

export class {{ pascalCase usecase_name }}CreateRequestDTO extends {{ pascalCase usecase_name }}RequestDTO {}

export class {{ pascalCase usecase_name }}UpdateRequestDTO {
  @IsString()
  @IsOptional()
  name?: string;
}

export class {{ pascalCase usecase_name }}ResponseDTO {
  @ApiProperty({ example: 999 })
  id: number;

  @ApiProperty({ example: 'Maisson Saraiva Moreira' })
  name: string;
}

export class {{ pascalCase usecase_name }}ListQueryDTO extends PaginationQueryDTO {
  @IsString()
  @IsOptional()
  name?: string;

  @IsUUID()
  @IsOptional()
  id?: string;
}

export class {{ pascalCase usecase_name }}ListResponseDTO extends PaginationResponseDTO<{{ pascalCase usecase_name }}ResponseDTO> {
  @ApiProperty({ isArray: true, type: {{ pascalCase usecase_name }}ResponseDTO })
  data: {{ pascalCase usecase_name }}ResponseDTO[];
}
