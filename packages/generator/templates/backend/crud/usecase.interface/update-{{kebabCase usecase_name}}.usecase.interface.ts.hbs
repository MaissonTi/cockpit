import { {{ pascalCase usecase_name }}Model } from '@/domain/models/{{ kebabCase usecase_name }}.model';

export namespace IUpdate{{ pascalCase usecase_name }}UseCase {
  export type Input = Partial<Omit<{{ pascalCase usecase_name }}Model, 'id' | 'createdAt' | 'updatedAt'>>;
  export type Output = {{ pascalCase usecase_name }}Model;
}

export interface IUpdate{{ pascalCase usecase_name }}UseCase {
  execute(id: string, data: IUpdate{{ pascalCase usecase_name }}UseCase.Input): Promise<{{ pascalCase usecase_name }}Model>;
}
