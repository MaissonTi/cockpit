import { {{ pascalCase usecase_name }}Model } from '@/domain/models/{{ kebabCase usecase_name }}.model';

export namespace IGet{{ pascalCase usecase_name }}UseCase {
  export type Output = {{ pascalCase usecase_name }}Model | null;
}

export interface IGet{{ pascalCase usecase_name }}UseCase {
  execute(id: string): Promise<IGet{{ pascalCase usecase_name }}UseCase.Output>;
}
