import { {{ pascalCase usecase_name }}Model } from '@/domain/models/{{ kebabCase usecase_name }}.model';
import { {{ pascalCase usecase_name }} as {{ pascalCase usecase_name }}Prisma } from '@prisma/client';

export class {{ pascalCase usecase_name }}Mapper {
  protected static objectModel(data: {{ pascalCase usecase_name }}Prisma) {
    return {
      id: data.id,
      name: data.name,
      createdAt: data.createdAt,
      updatedAt: data.updatedAt,
    };
  }

  protected static objectPrisma(data: {{ pascalCase usecase_name }}Model) {
    return {
      id: data.id,
      name: data.name,
      createdAt: data.createdAt,
      updatedAt: data.updatedAt,
    };
  }

  static toModel(data: {{ pascalCase usecase_name }}Prisma): {{ pascalCase usecase_name }}Model | null {
    return data && {{ pascalCase usecase_name }}Mapper.objectModel(data);
  }

  static toPrisma(data: {{ pascalCase usecase_name }}Model): {{ pascalCase usecase_name }}Prisma | null {
    return data && {{ pascalCase usecase_name }}Mapper.objectPrisma(data);
  }
}
